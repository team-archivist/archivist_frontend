import { DropdownMenu } from "@radix-ui/themes";
import React, { useState } from "react";

type Props = {};

const Dropdown = ({ open, onOpen }: Props) => {
  return <div>hello</div>;
};

export default Dropdown;
